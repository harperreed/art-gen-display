import { createHash } from 'crypto';
export function SHA256(data) {
  return {
    toString() {
      return createHash('sha256').update(data).digest('hex');
    }
  };
}
export const enc = { Hex: 'hex' };
export default { SHA256, enc };
